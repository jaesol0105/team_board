<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Teamboard" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/default_background</item>
        <item name="colorPrimaryVariant">@color/default_background</item>
        <item name="colorOnPrimary">@color/default_background</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/default_background</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/default_background</item>
        <item name="android:navigationBarColor" >@color/default_background</item>
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/teal_200</item>
    </style>

    <!-- BottomSheetDialog -->
    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!-- Transparent Background Dialog -->
    <style name="transparentDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
    </style>
</resources>